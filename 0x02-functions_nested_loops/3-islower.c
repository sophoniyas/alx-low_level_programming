#include "main.h"

/**
 * _islower - Checks for lowercase character
 * @c: character to be checked
 * Return: 1 for lowaercase character or 0 for anything else
 */
int _islower(char c)
{
	if (c >= 97 && c <= 122)
	{
		
	return (1);
	}
	return (0);
}
